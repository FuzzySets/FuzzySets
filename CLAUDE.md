# CLAUDE.md

このファイルは、このリポジトリで作業する際に Claude Code が従うべきルールを定義します。

## コード作成時のルール

- **ヘッダー・フッターの付属確認**: ページを新規作成・変更する際は、ヘッダー・フッターが付属しているか必ずチェックすること。
  - 具体的には `<head>` で `js/include.js` を読み込み、`<div id="header-container" data-page="xxx"></div>` と `<div id="footer-container"></div>` が配置されているかを確認する（詳細は README.md 参照）。
  - 例外的にヘッダー・フッターが不要なページについては、その都度ユーザーから知らされる。
- **スマートフォン表示の確認**: 指示があったタイミングで、スマートフォンでの表示が崩れていないことを必ずチェックする。
- **依頼範囲外の変更**: コードレビューや実装上の観点から、依頼されたタスク外の変更を加えることが推奨される場合は、先にユーザーに確認を取ってから行う。無断で範囲外の変更を加えない。
